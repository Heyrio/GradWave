import { width } from "@material-ui/system";

export const styles = theme => ({
    listContainer:{
        padding: 25, 
        height: 'calc(100vh - 70px)', 
        width: '100%', 
        marginRight: 20,
        backgroundColor: "#fff",
        color: '#232323'
    }


});